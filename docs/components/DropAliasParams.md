# DropAliasParams

删除别名。对应组件：`custom.components.DropAliasComp`

## 参数

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:----:|--------|------|
| `databaseName` | String | 否 | `""` | |
| `alias` | String | 是 | | 要删除的别名 |

## JSON 示例

```json
{"DropAliasParams_0": {"alias": "my_alias"}}
```
