# WaitParams

等待指定时长。对应组件：`custom.components.WaitComp`

## 参数

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:----:|--------|------|
| `waitMinutes` | long | 是 | `1` | 等待时长（分钟） |

## JSON 示例

```json
{"WaitParams_0": {"waitMinutes": 5}}
```
