# QuerySegmentInfoParams

获取 Query Segment 信息（V1 API）。对应组件：`custom.components.QuerySegmentInfoComp`

## 参数

| 字段 | 类型 | 必填 | 默认值 | 说明 |
|------|------|:----:|--------|------|
| `collectionName` | String | 否 | `""` | |

## JSON 示例

```json
{"QuerySegmentInfoParams_0": {"collectionName": ""}}
```
